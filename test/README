test/ — PlatformIOテスト

- native/: ホストPC上で実行するmotionロジックのテスト(pio test -e native)。
  Arduino非依存の純ロジック(CoreXYKinematics、SegmentGenerator等)を対象とする。
  test/native/Arduino.h はテスト用の最小スタブ。

実行方法:
  pio test -e native

ホストツール(Python)のテストはリポジトリ直下のtests/(pytest)にある。
